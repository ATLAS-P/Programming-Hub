if groups.length == 0
    p You are currently not enrolled in any classes.
else
    .row.groups
        for group in groups
            .col-lg-4.col-sm-6
                .group(group=group.id)
                    h3.special= group.name
                    if group.openAssignments > 0
                        if group.open == 1
                            p There is 1 open assignment.
                        else 
                            p There are #{group.openAssignments} open assignments.
                        p The next deadline is in:
                        date(format="in")= group.nextDeadline
                    else 
                        p There are no open assignments.